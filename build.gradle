plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'org.seleniumhq.selenium', name: 'selenium-java', version: '4.17.0'
    implementation group: 'org.testng', name: 'testng', version: '7.9.0'
    implementation group: 'com.opencsv', name: 'opencsv', version: '5.9'
    implementation group: 'ch.qos.logback', name: 'logback-classic', version: '1.4.14'
}
processTestResources {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
test {
    useTestNG()
}
sourceSets {
    test {
        resources {
            srcDir 'src/test/resources'
        }
    }
}
def registerTask(String taskName, String suitePath) {
    tasks.register(taskName, Test) {
        useTestNG() {
            suites suitePath
        }
        if (project.hasProperty('browser')) {
            systemProperty 'browser', "${browser}"
        }
        if (project.hasProperty('mode')) {
            systemProperty 'mode', "${mode}"
        }
    }
}

registerTask('allcom_qa_regression', 'src/test/resources/regression.xml')
registerTask('allcom_qa_login', 'src/test/resources/login_tests.xml')
registerTask('allcom_qa_register', 'src/test/resources/registration_tests.xml')
registerTask('allcom_qa_aboutUs', 'src/test/resources/about_us_tests.xml')
registerTask('allcom_qa_header_page', 'src/test/resources/header_page_tests.xml')
registerTask('allcom_qa_restore_password_page', 'src/test/resources/restore_password_page_tests.xml')